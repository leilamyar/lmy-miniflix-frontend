import { ToListIconDirective } from './to-list-icon.directive';

describe('ToListIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ToListIconDirective();
    expect(directive).toBeTruthy();
  });
});
